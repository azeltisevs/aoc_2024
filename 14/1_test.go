package main

import (
	"advent_of_code_2024/helpers"
	"testing"
)

func TestTask1(t *testing.T) {
	lines := helpers.ExtractAllNumbers(helpers.ReadAllLinesFromFile("./input.txt"))

	robots := make(map[Point]helpers.Point)
	for numbers := range lines {

	}
}
